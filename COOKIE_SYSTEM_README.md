# מערכת ניהול עוגיות (Cookies) לאתר Paintz

## מה הוספנו לאתר?

### קבצים חדשים שנוצרו:

1. **`cookie-consent.js`** - הסקריפט הראשי לניהול העוגיות
2. **`COOKIE_SYSTEM_README.md`** - המדריך הזה

**הערה:** מדיניות העוגיות תיכתב בתנאי השימוש של האתר.

### שינויים בקבצים קיימים:

- **`homepage.html`** - נוסף הסקריפט
- **`shop.html`** - נוסף הסקריפט
- **`about.html`** - נוסף הסקריפט  
- **`contact.html`** - נוסף הסקריפט
- **`privacy-policy.html`** - עודכן הסעיף על עוגיות ונוסף הסקריפט

## איך זה עובד?

### 1. באנר הסכמה
- מופיע אוטומטיט למבקרים חדשים
- מאפשר לבחור בין קבלת הכל, דחיית הכל, או התאמה אישית
- נשמר למשך שנה בדפדפן

### 2. סוגי עוגיות
- **חיוניות** - תמיד פעילות (עגלת קניות, אבטחה)
- **אנליטיקה** - Google Analytics (אופציונלי)
- **שיווק** - פרסומות ממוקדות (אופציונלי)
- **פונקציונליות** - העדפות משתמש (אופציונלי)

### 3. בחירה סופית
- ברגע שהמשתמש בוחר (קבל הכל/דחה/התאמה אישית), הבחירה נשמרת סופית
- לא ניתן לשנות דרך האתר - רק דרך הגדרות הדפדפן

## הגדרות נוספות שכדאי לעשות:

### 1. Google Analytics
בקובץ `cookie-consent.js` שורה 133, החלף את:
```javascript
const GA_ID = 'G-XXXXXXXXXX';
```
עם המזהה האמיתי של Google Analytics שלך.

### 2. כתיבת מדיניות עוגיות בתנאי השימוש
הוסף סעיף על עוגיות בקובץ `terms-of-service.html` שיכלול:
- הסבר על סוגי העוגיות השונים
- זכויות המשתמש לניהול העוגיות
- מידע על צדדים שלישיים

### 3. הוספת סקריפטים נוספים
אם יש לך סקריפטים נוספים (כמו Facebook Pixel, Google Ads), הוסף אותם בפונקציות:
- `loadMarketingScripts()` - לסקריפטים שיווקיים
- `loadFunctionalScripts()` - לסקריפטים פונקציונליים

## בדיקה שהכל עובד:

1. **פתח את האתר** - אמור להופיע באנר עוגיות
2. **בחר "התאמה אישית"** - אמור להציג אפשרויות מפורטות
3. **בחר אפשרות כלשהי** - הבאנר אמור להיעלם ולא לחזור
4. **בדוק ב-Developer Tools** - `document.cookie` אמור להציג את ההעדפות עם `"final":true`

## תאימות לחוקים:

המערכת תואמת ל:
- **GDPR** (תקנת הגנת המידע האירופית)
- **חוק הגנת הפרטיות הישראלי**
- **ePrivacy Directive**

## תמיכה טכנית:

המערכת תומכת ב:
- כל הדפדפנים המודרניים
- מכשירים ניידים
- קוראי מסך (נגישות)
- RTL (עברית)

## שינויים עתידיים:

אם תרצי להוסיף עוגיות חדשות או לשנות התנהגות:
1. עדכן את הקובץ `cookie-consent.js`
2. עדכן את `cookie-policy.html` בהתאם
3. בדוק שהכל עובד תקין

---

**הערה חשובה:** זכור לבדוק את המערכת באופן קבוע ולעדכן את מדיניות העוגיות בהתאם לשינויים באתר או בחקיקה.
